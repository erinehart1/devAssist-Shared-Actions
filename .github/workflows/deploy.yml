# === .github/workflows/deploy.yml ===
# TEMP TEST WORKFLOW — Just prints a secret from consumer repo to validate
name: Debug Secret Injection

on:
  workflow_call:
    secrets:
      SF_JWT_KEY:
        required: true

jobs:
  test:
    name: Secret Echo
    runs-on: ubuntu-latest

    steps:

      - name: Check secret length
        run: |
          echo "Length: ${#SF_JWT_KEY}"
        env:
          SF_JWT_KEY: ${{ secrets.SF_JWT_KEY }}


      - name: Print secret value
        run: |
          echo "Secret contents: $SF_JWT_KEY"
        env:
          SF_JWT_KEY: ${{ secrets.SF_JWT_KEY }}

# All other logic temporarily removed for clarity while testing
